#version 430

layout( local_size_x = 1024, local_size_y = 1, local_size_z = 1 ) in;

layout( std430, binding = 0 ) buffer VertexBuffer {
    vec3 positions[];
};

void main() {
    uint gid = gl_GlobalInvocationID.x;
    if (gid > positions.length()) return;

    positions[index] += vec3(5.0);
}